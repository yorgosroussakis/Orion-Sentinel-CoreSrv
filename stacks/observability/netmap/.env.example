# ============================================================================
# NetAlertX Network Mapping Configuration
# ============================================================================
#
# Copy this file to .env and customize for your deployment:
#   cp .env.example .env
#
# ============================================================================

# ============================================================================
# SCAN CONFIGURATION
# ============================================================================

# Scan interval in minutes (how often to scan the network)
# Default: 5 minutes
# Recommended: 5-15 for active monitoring, 30+ for passive monitoring
NETALERTX_SCAN_INTERVAL=5

# Network subnet to scan
# Format: 192.168.1.0/24 or 10.0.0.0/16
# Leave empty for auto-detection (scans all local subnets)
# Examples:
#   NETALERTX_SCAN_SUBNET=192.168.1.0/24
#   NETALERTX_SCAN_SUBNET=10.0.8.0/24
NETALERTX_SCAN_SUBNET=

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Auto-detection works well for most home networks
# 2. Specify subnet if you have multiple networks and want to scan a specific one
# 3. Lower scan intervals provide more real-time detection but use more CPU
# 4. NetAlertX requires NET_ADMIN and NET_RAW capabilities for network scanning
# 5. Runs in host network mode for direct network access
#
# After first run, configure additional settings in the web UI:
#   - Device naming and grouping
#   - Alert notifications (email, webhook, etc.)
#   - MAC vendor database updates
#   - Network device categories
#
# ============================================================================
