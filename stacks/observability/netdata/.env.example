# ============================================================================
# Netdata Configuration
# ============================================================================
#
# Netdata provides real-time performance monitoring for systems and containers.
#
# Quick Start:
#   1. Copy this file: cp .env.example .env
#   2. Edit hostname and domain (required)
#   3. Optionally configure Netdata Cloud (for remote access)
#   4. Deploy: docker compose up -d
#
# ============================================================================

# ============================================================================
# BASIC CONFIGURATION
# ============================================================================

# Timezone (should match your host system)
TZ=Europe/Amsterdam

# Hostname shown in Netdata dashboard
NETDATA_HOSTNAME=coresrv-netdata

# Domain for Traefik access
# Examples:
#   - netdata.orion.lan (local)
#   - netdata.example.com (public)
NETDATA_DOMAIN=netdata.orion.lan

# Local domain (inherited from main .env if using compose include)
LOCAL_DOMAIN=orion.lan

# ============================================================================
# STORAGE PATHS
# ============================================================================

# Root path for internal storage (configs, databases)
# This should match your main Orion configuration
ORION_INTERNAL_ROOT=/srv/orion/internal

# Netdata stores its data in:
#   - ${ORION_INTERNAL_ROOT}/observability/netdata/config  - Configuration
#   - ${ORION_INTERNAL_ROOT}/observability/netdata/lib     - Persistent data
#   - ${ORION_INTERNAL_ROOT}/observability/netdata/cache   - Metrics cache

# ============================================================================
# NETDATA CLOUD (OPTIONAL)
# ============================================================================
#
# Netdata Cloud provides:
#   - Remote access to your dashboards
#   - Multiple node management
#   - Alerts and notifications
#   - Team collaboration
#
# To enable:
#   1. Sign up at https://app.netdata.cloud
#   2. Get claim token from "Add Nodes" → "Docker"
#   3. Set NETDATA_CLAIM_TOKEN below
#   4. Optionally set NETDATA_CLAIM_ROOMS
#
# To disable:
#   - Leave NETDATA_CLAIM_TOKEN empty (default)
#   - Netdata will work locally only
#
# ============================================================================

# Claim token from Netdata Cloud (leave empty to disable cloud)
NETDATA_CLAIM_TOKEN=

# Space ID / Room ID from Netdata Cloud (optional)
NETDATA_CLAIM_ROOMS=

# Netdata Cloud URL (usually don't need to change)
NETDATA_CLAIM_URL=https://app.netdata.cloud

# ============================================================================
# PRIVACY & TELEMETRY
# ============================================================================

# Disable anonymous statistics (1 = disabled, 0 = enabled)
# When enabled, Netdata sends anonymous usage statistics
# Recommended: Keep disabled (1) for privacy
NETDATA_DISABLE_TELEMETRY=1

# ============================================================================
# NOTES
# ============================================================================
#
# Host Access:
#   Netdata needs access to host /proc, /sys, and Docker socket to collect
#   metrics. All mounts are read-only for security.
#
# Security:
#   - Access is secured via Traefik reverse proxy
#   - Add Authelia middleware for authentication (see README)
#   - Netdata runs with minimal required capabilities
#
# Performance:
#   Netdata is lightweight (~50-100MB RAM, <1% CPU)
#   Metrics are stored locally for 1-2 hours by default
#   Adjust retention in netdata.conf if needed
#
# Documentation:
#   - https://learn.netdata.cloud/docs
#   - Local README: stacks/observability/netdata/README.md
#
# ============================================================================
