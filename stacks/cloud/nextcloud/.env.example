# ============================================================================
# Nextcloud Environment Configuration
# ============================================================================
#
# Copy this file to .env and customize for your deployment:
#   cp .env.example .env
#
# ============================================================================

# ============================================================================
# NEXTCLOUD ADMIN ACCOUNT
# ============================================================================

# Admin username (used only during initial setup)
NEXTCLOUD_ADMIN_USER=admin

# Admin password (REQUIRED - change this!)
# Generate with: openssl rand -base64 32
NEXTCLOUD_ADMIN_PASSWORD=changeme-secure-password-here

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Database name
NEXTCLOUD_DB_NAME=nextcloud

# Database user
NEXTCLOUD_DB_USER=nextcloud

# Database password (REQUIRED - change this!)
# Generate with: openssl rand -base64 32
NEXTCLOUD_DB_PASSWORD=changeme-secure-db-password

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================

# Redis password for caching and file locking
# Generate with: openssl rand -base64 32
NEXTCLOUD_REDIS_PASSWORD=redis-change-me

# ============================================================================
# PERFORMANCE TUNING
# ============================================================================

# PHP memory limit (adjust based on your usage)
# Recommended: 512M for small deployments, 1G+ for many users
NEXTCLOUD_PHP_MEMORY_LIMIT=512M

# Maximum upload file size
# Adjust based on your needs (photos, videos, etc.)
NEXTCLOUD_PHP_UPLOAD_LIMIT=10G

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Change all "changeme" passwords before production use
# 2. The admin account is created only on first run
# 3. Use strong, unique passwords (20+ characters recommended)
# 4. For large file uploads, also check Traefik proxy settings
# 5. Redis improves performance significantly for multi-user setups
# 6. Store this file securely - never commit to version control
#
# After first setup, configure additional settings in Nextcloud admin panel:
# - Email server (for password resets and notifications)
# - Two-factor authentication
# - External storage (if needed)
# - Recommended apps (Calendar, Contacts, Photos)
#
# ============================================================================
