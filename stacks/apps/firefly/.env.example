# ============================================================================
# Firefly III Environment Configuration
# ============================================================================
#
# Copy this file to .env and customize for your deployment:
#   cp .env.example .env
#
# ============================================================================

# ============================================================================
# FIREFLY III APPLICATION
# ============================================================================

# APP_KEY - 32 character random string (REQUIRED)
# Generate with: head /dev/urandom | LC_ALL=C tr -dc 'A-Za-z0-9' | head -c 32 && echo
# Or use: docker run --rm fireflyiii/core:latest php artisan key:generate --show
FIREFLY_APP_KEY=SomeRandomStringOf32CharsExactly

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Database name
FIREFLY_DB_NAME=firefly

# Database user
FIREFLY_DB_USER=firefly

# Database password (REQUIRED - change this!)
# Generate with: openssl rand -base64 32
FIREFLY_DB_PASSWORD=changeme-secure-password-here

# ============================================================================
# FIREFLY DATA IMPORTER
# ============================================================================

# Personal Access Token for Data Importer
# Generate this in Firefly III after initial setup:
#   1. Login to Firefly III
#   2. Go to Options -> Profile -> OAuth
#   3. Create new Personal Access Token
#   4. Copy token here
FIREFLY_IMPORTER_TOKEN=

# ============================================================================
# MAIL CONFIGURATION (Optional)
# ============================================================================
# Configure if you want email notifications

# Mail driver (log, smtp, sendmail)
FIREFLY_MAIL_MAILER=log

# SMTP settings (only needed if using smtp)
FIREFLY_MAIL_HOST=
FIREFLY_MAIL_PORT=587
FIREFLY_MAIL_FROM=firefly@orion.lan
FIREFLY_MAIL_USERNAME=
FIREFLY_MAIL_PASSWORD=
FIREFLY_MAIL_ENCRYPTION=tls

# ============================================================================
# BANK IMPORT INTEGRATIONS (Optional)
# ============================================================================

# Nordigen (GoCardless) - Free EU bank connections
# Sign up at: https://nordigen.com/en/account/login/
FIREFLY_NORDIGEN_ID=
FIREFLY_NORDIGEN_KEY=

# Spectre (Salt Edge) - Paid bank connections
# Sign up at: https://www.saltedge.com/products/spectre
FIREFLY_SPECTRE_APP_ID=
FIREFLY_SPECTRE_SECRET=

# ============================================================================
# NOTES
# ============================================================================
#
# 1. APP_KEY must be exactly 32 characters
# 2. Change all "changeme" passwords before production use
# 3. Store this file securely - it contains sensitive credentials
# 4. Never commit this file to version control (.gitignore should exclude it)
# 5. Generate FIREFLY_IMPORTER_TOKEN after first Firefly III setup
#
# ============================================================================
