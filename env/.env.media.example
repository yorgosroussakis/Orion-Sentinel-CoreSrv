# Media Stack Configuration
# Jellyfin + *arr + qBittorrent + VPN + Jellyseerr + Recommendarr

# ============================================================================
# USER/GROUP & TIMEZONE
# ============================================================================
# These should match your host user that owns the media files
# Run `id` on your host to find your PUID and PGID
PUID=1000
PGID=1000
TZ=Europe/Amsterdam

# ============================================================================
# HOST DIRECTORY LAYOUT (Trash-Guides compatible, hardlink-friendly)
# ============================================================================
# This layout supports atomic moves and hardlinks between torrents and library.
# The key is that downloads and library share the same parent filesystem.
#
# Example structure:
# /srv/orion-sentinel-core/
#   ├── config/                          ← CONFIG_ROOT
#   │   ├── jellyfin/
#   │   ├── sonarr/
#   │   ├── radarr/
#   │   └── ...
#   ├── data/                            ← DATA_ROOT
#   └── media/                           ← Media parent
#       ├── torrents/                    ← DOWNLOAD_ROOT (qBittorrent downloads here)
#       │   ├── movies/
#       │   └── tv/
#       └── library/                     ← Final media library (Jellyfin reads here)
#           ├── movies/
#           └── tv/
#
# With this layout, when Sonarr/Radarr import from torrents/ to library/,
# they use hardlinks (instant, no copy) instead of moving files.

CONFIG_ROOT=/srv/orion-sentinel-core/config
DATA_ROOT=/srv/orion-sentinel-core/data
DOWNLOAD_ROOT=/srv/orion-sentinel-core/media/torrents
MEDIA_LIBRARY_ROOT=/srv/orion-sentinel-core/media/library

# Specific paths for media services
TV_ROOT=/srv/orion-sentinel-core/media/library/tv
MOVIES_ROOT=/srv/orion-sentinel-core/media/library/movies

# ============================================================================
# VPN CONFIGURATION (Gluetun for ProtonVPN)
# ============================================================================
# Provider: Use 'protonvpn' for ProtonVPN
VPN_IMAGE=qmcgaw/gluetun:latest
VPN_SERVICE_PROVIDER=protonvpn
VPN_TYPE=wireguard

# ProtonVPN WireGuard credentials
# Get these from your ProtonVPN account settings
# See: https://account.protonvpn.com/account#openvpn
VPN_WIREGUARD_PRIVATE_KEY=your_wireguard_private_key_here
VPN_WIREGUARD_ADDRESS=10.2.0.2/32

# ProtonVPN server selection
# Options: Netherlands, Switzerland, Sweden, Iceland, etc.
# Leave empty for automatic selection
VPN_COUNTRY=Netherlands

# Local network CIDR (allows LAN access to qBittorrent through VPN)
# Adjust to match your LAN subnet (e.g., 192.168.1.0/24)
VPN_LOCAL_NETWORK=192.168.1.0/24

# ============================================================================
# JELLYFIN
# ============================================================================
JELLYFIN_HOST=jellyfin.local
JELLYFIN_PUBLISHED_SERVER_URL=https://jellyfin.local
JELLYFIN_BASE_URL=http://jellyfin:8096
JELLYFIN_API_KEY=generate_this_after_first_startup

# ============================================================================
# SONARR
# ============================================================================
SONARR_BASE_URL=http://sonarr:8989
SONARR_API_KEY=generate_this_after_first_startup

# ============================================================================
# RADARR
# ============================================================================
RADARR_BASE_URL=http://radarr:7878
RADARR_API_KEY=generate_this_after_first_startup

# ============================================================================
# PROWLARR
# ============================================================================
PROWLARR_HOST=prowlarr.local
PROWLARR_API_KEY=generate_this_after_first_startup

# ============================================================================
# BAZARR (Optional - for subtitles)
# ============================================================================
BAZARR_API_KEY=generate_this_after_first_startup

# ============================================================================
# JELLYSEERR
# ============================================================================
JELLYSEERR_HOST=requests.local
JELLYSEERR_API_KEY=generate_this_after_first_startup

# ============================================================================
# RECOMMENDARR (Media AI)
# ============================================================================
# Recommendarr connects to Sonarr, Radarr, and Jellyfin to provide AI-powered
# media recommendations based on your viewing habits.

RECOMMENDARR_HOST=recommend.local

# Optional: Trakt.tv integration for enhanced recommendations
# Create an app at https://trakt.tv/oauth/applications
TRAKT_CLIENT_ID=
TRAKT_CLIENT_SECRET=
TRAKT_ACCESS_TOKEN=

# ============================================================================
# QBITTORRENT
# ============================================================================
# qBittorrent runs inside the VPN container for privacy
# WebUI is accessible via VPN container port forwarding

# WebUI port (exposed from VPN container)
QBITTORRENT_WEBUI_PORT=8080

# Connection settings
QBITTORRENT_BT_PORT=6881
