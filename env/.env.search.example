# Search Stack Configuration
# SearXNG - Privacy-respecting metasearch engine

# ============================================================================
# USER/GROUP & TIMEZONE
# ============================================================================
PUID=1000
PGID=1000
TZ=Europe/Amsterdam

# ============================================================================
# HOST DIRECTORY LAYOUT
# ============================================================================
CONFIG_ROOT=/srv/orion-sentinel-core-sentinel-core/config

# ============================================================================
# SEARXNG
# ============================================================================
# Base URL (used for generating correct links)
SEARXNG_BASE_URL=https://search.local

# Instance name (appears in page title and footer)
SEARXNG_INSTANCE_NAME=Orion Search

# Secret key (generate with: openssl rand -hex 32)
# Used for encrypting cookies and sessions
SEARXNG_SECRET_KEY=change_me_to_a_random_hex_string_use_openssl_rand_hex_32

# ============================================================================
# PRIVACY & SECURITY SETTINGS
# ============================================================================
# These can be overridden in search/searxng/settings.yml

# Enable image proxy (prevents direct image loading from search results)
SEARXNG_IMAGE_PROXY=true

# Enable HTTP method 'GET' for search queries (useful for sharing links)
# Set to 'false' for more privacy (POST only)
SEARXNG_QUERY_METHOD_GET=true

# Safe search filter (0=off, 1=moderate, 2=strict)
SEARXNG_SAFE_SEARCH=0

# Autocomplete (set to empty string to disable)
SEARXNG_AUTOCOMPLETE=google

# ============================================================================
# LIMITER (rate limiting / bot protection)
# ============================================================================
# Protect against abuse while allowing legitimate use

# Enable rate limiting
SEARXNG_LIMITER=false

# ============================================================================
# DEFAULT SEARCH ENGINES
# ============================================================================
# Configure preferred engines in search/searxng/settings.yml
# Popular options:
#   - google, duckduckgo, brave, bing
#   - wikipedia, wikidata
#   - github, stackoverflow, reddit
#   - youtube, invidious
#   - archive.org
#   - And many more...
#
# You can enable/disable engines and set their weights in the config file

# ============================================================================
# RESULT PREFERENCES
# ============================================================================
# Default preferences (users can override in UI)
# Configure in settings.yml:
#   - Language: en-US
#   - Results per page: 20
#   - Theme: simple (or oscar, legacy)
#   - Disable telemetry/tracking: yes
